<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white"/> <!-- Obligatorio para que clipToOutline funcione -->
    <corners android:radius="16dp"/>
</shape>